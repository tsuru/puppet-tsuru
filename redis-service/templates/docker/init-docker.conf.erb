description     "Docker daemon"

start on filesystem and started lxc-net
stop on runlevel [!2345]

respawn

script
/usr/bin/docker -r -d <%= @filer_docker ? "-g "+filer_docker : '' %> -H 0.0.0.0:4243
end script


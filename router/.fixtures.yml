fixtures:
  forge_modules:
    base:
      repo: "tsuru/base"
      ref: 0.0.2
    sudo:
      repo: "saz/sudo"
      ref: 3.0.6
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: 4.3.2
    apt:
      repo: "puppetlabs/apt"
      ref: 1.7.0
  symlinks:
    "router": "#{source_dir}"
